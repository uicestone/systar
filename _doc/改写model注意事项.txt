sql语句用双引号包围
sql语句中的sql关键字大写，其他小写，保留字字段名和表名加``反引号，如case,group,order
sql语句中所有的整形php变量在方法最初用intval()处理一遍，语句中不加单引号，牵涉到[]或->的用{}包围,如...id = $id...
sql语句中除php数值变量以外的其他变量，并用''包围，如...term = '{$this->config->item('term')}'...
sql语句中的php语句计算值（非变量），作为字符串连接，如...WHERE '".$this->user->isLogged()."' = '1'...